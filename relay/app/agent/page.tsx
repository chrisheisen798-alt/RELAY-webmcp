import { AppShell } from "@/components/app-shell";
export default function Agent(){return <AppShell active="Agent"><main className="page"><div className="page-header"><div><div className="eyebrow">Agent workspace</div><h1>Work with an agent, deliberately.</h1><p className="intro">Relay will make agent plans, tool calls, and approval boundaries visible before execution.</p></div><span className="pill">Not connected</span></div><section className="agent-panel"><div className="agent-status"><span className="status-dot"/>Agent connection is not configured</div><div className="agent-body"><div><strong style={{color:"#344054",display:"block",marginBottom:5}}>No agent is running</strong>This foundation intentionally contains no AI integration, simulated work, or WebMCP tool calls.</div></div><div className="agent-form"><input aria-label="Instruction" placeholder="Connect an agent to send instructions" disabled/><button className="button" type="button" disabled>Send</button></div></section></main></AppShell>}
