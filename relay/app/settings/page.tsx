import { AppShell } from "@/components/app-shell";
export default function Settings(){return <AppShell active="Settings"><main className="page"><div className="eyebrow">Workspace settings</div><h1>Settings</h1><p className="intro">Authentication, organization membership, and workspace preferences will be configured through Supabase.</p><section className="card" style={{marginTop:30}}><h2 className="section-title">Setup status</h2><p className="intro" style={{marginTop:10}}>Add the Supabase environment variables described in <code>.env.example</code> and apply the included migration before enabling data access.</p></section></main></AppShell>}
