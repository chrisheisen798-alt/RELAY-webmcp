import Link from "next/link";
const items = [["▦","Dashboard","/dashboard"],["□","Projects","/projects"],["◌","Agent","/agent"],["✓","Approvals","/approvals"],["≡","Activity","/activity"],["⚙","Settings","/settings"]] as const;
export function AppShell({children,active}:{children:React.ReactNode;active?:string}){return <div className="shell"><aside className="sidebar"><Link href="/dashboard" className="brand"><span className="brand-mark">R</span>Relay</Link><nav className="nav" aria-label="Main navigation"><span className="nav-label">Workspace</span>{items.map(([icon,name,href])=><Link key={href} href={href} className={`nav-link ${active===name?"active":""}`}><span className="nav-icon" aria-hidden="true">{icon}</span>{name}</Link>)}</nav><div className="sidebar-foot"><b>Foundation phase</b><br/>The live agent and WebMCP capabilities are intentionally not connected yet.</div></aside><div className="content"><header className="topbar"><div className="workspace">Relay workspace <span>· Product team</span></div><input className="search" aria-label="Search workspace" placeholder="Search projects, tasks, and documents" disabled/><button className="icon-button" aria-label="Notifications" type="button">◦</button><span className="avatar" aria-label="Current user">JD</span></header>{children}</div></div>}
