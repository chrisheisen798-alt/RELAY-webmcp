import { afterEach,describe,expect,it,vi } from "vitest";
import { registerProjectTools } from "./register-tools";
describe("WebMCP registration boundary",()=>{afterEach(()=>vi.unstubAllGlobals());it("reports unavailable without the browser API",async()=>{vi.stubGlobal("document",{});await expect(registerProjectTools("project-a",new AbortController().signal)).resolves.toMatchObject({support:"unavailable",registeredToolCount:0});});it("registers the real named tool and confirms discovery",async()=>{const registerTool=vi.fn().mockResolvedValue(undefined);const getTools=vi.fn().mockResolvedValue([{name:"search_project_resources",description:"",inputSchema:"{}",origin:"https://relay.test"}]);vi.stubGlobal("document",{modelContext:{registerTool,getTools}});const status=await registerProjectTools("project-a",new AbortController().signal);expect(registerTool.mock.calls[0][0].name).toBe("search_project_resources");expect(status).toMatchObject({support:"registered",registeredToolCount:1});});});
